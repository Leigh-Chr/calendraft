# ===================================
# Calendraft - Docker Ignore
# Exclude unnecessary files from Docker builds
# ===================================

# Dependencies (will be installed in container)
node_modules
.pnp
.pnp.js

# Testing (not needed in production)
coverage
.nyc_output
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
__tests__
**/__tests__
vitest.config.*

# Build outputs (will be rebuilt in Docker)
dist
build
*.tsbuildinfo
dev-dist

# Development files
.env
.env.local
.env.development
.env.test
.env.production
.vscode
.idea
.cursor
*.swp
*.swo
*~

# Git (not needed in image)
.git
.gitignore
.gitattributes

# Documentation (not needed in image)
*.md
!README.md
docs

# CI/CD (not needed in image)
.github
.gitlab-ci.yml
.travis.yml

# Lock files from other package managers (not used with Bun)
package-lock.json
yarn.lock
pnpm-lock.yaml
# Note: bun.lock is needed for --frozen-lockfile, so it's NOT ignored

# Configuration files (not needed in image)
bunfig.toml
knip.config.ts
bts.jsonc
ruler.config.*

# Misc
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
bun-debug.log*

# Docker files themselves
Dockerfile
docker-compose*.yml
.dockerignore

# Database files (should use volumes)
*.db
*.sqlite
*.sqlite3
local.db

# Turbo cache
.turbo

# Temporary files
tmp
temp
*.tmp

# Better-T-Stack
.alchemy
