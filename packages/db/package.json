{
	"name": "@calendraft/db",
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		},
		"./*": {
			"types": "./src/*.ts",
			"default": "./src/*.ts"
		}
	},
	"scripts": {
		"build": "tsdown",
		"typecheck": "echo 'Skipping typecheck for @calendraft/db (Prisma generated files)'",
		"clean": "rm -rf dist *.tsbuildinfo",
		"db:push": "prisma db push",
		"db:studio": "prisma studio",
		"db:generate": "prisma generate",
		"db:migrate": "prisma migrate dev",
		"db:seed": "bun run prisma/seed.ts"
	},
	"prisma": {
		"seed": "bun run prisma/seed.ts"
	},
	"devDependencies": {
		"@calendraft/config": "workspace:*",
		"prisma": "^7.1.0",
		"tsdown": "catalog:"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@noble/hashes": "^2.0.1",
		"@prisma/adapter-pg": "^7.1.0",
		"@prisma/client": "catalog:",
		"dotenv": "catalog:",
		"zod": "catalog:"
	}
}
