[install]
linker = "isolated"

# Security Scanner API: Scanne automatiquement les packages avant installation
# Bloque les packages vulnérables avant leur installation
#
# NOTE: Désactivé temporairement - cause un échec silencieux avec bun 1.3.4
# Le scanner bun-osv-scanner échoue silencieusement lors de l'installation,
# provoquant un code de sortie 1 sans message d'erreur visible.
#
# Pour réactiver le scanner:
# 1. Vérifier les issues connues: https://github.com/oven-sh/bun/issues
# 2. Tester avec une version plus récente de bun-osv-scanner
# 3. Ou utiliser un autre scanner compatible: @socketsecurity/bun-security-scanner
# 4. Décommenter les lignes ci-dessous
#
# [install.security]
# scanner = "bun-osv-scanner"
